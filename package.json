{
  "name": "jest-restful_booker",
  "version": "1.0.0",
  "description": "api automation",
  "main": "index.js",
  
  "scripts": {
    "test": "jest",
    "test-reports": "jest --testPathPattern=supertest.test --reporters=default --reporters=jest-html-reporter",
    "test-reports-mocked": "jest --testPathPattern=moked.test --reporters=default --reporters=jest-html-reporter",
    "test:watch": "npm run test -- --watch"
  },
  "author": "Angela",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-html-reporter": "^3.10.2",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "date-and-time": "^3.3.0"
  }
}
